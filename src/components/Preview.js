import React from "react";
import styled from "styled-components";

const PreviewWrapper = styled.div``;

const Preview = () => {
  return (
    <PreviewWrapper>
      <img src="/assets/bread-butter.png" alt="bread butter" />
    </PreviewWrapper>
  );
};

export default Preview;
